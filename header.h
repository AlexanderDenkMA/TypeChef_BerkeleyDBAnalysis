#ifdef NOMPTEMP
  #define NOMPTEMP 1
#else
  #define NOMPTEMP 0
#endif
#ifdef MPTEMP_RENEW
  #define MPTEMP_RENEW 1
#else
  #define MPTEMP_RENEW 0
#endif
#ifdef MPTEMP_CHECK
  #define MPTEMP_CHECK 1
#else
  #define MPTEMP_CHECK 0
#endif
#ifdef MPTEMP_DEBUG
  #define MPTEMP_DEBUG 1
#else
  #define MPTEMP_DEBUG 0
#endif
